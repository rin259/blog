{
	// Base config read by @astrojs/cloudflare; the adapter generates the full
	// deploy config at dist/server/wrangler.json (assets → dist/client) during build.
	"name": "firefly-blog",
	"compatibility_date": "2025-01-01",
	"compatibility_flags": ["nodejs_compat"]
}
